package kpn.lib.services;

import kpn.lib.deleter.DeleteService;
import kpn.lib.loader.LoadService;
import kpn.lib.predicate.PredicateExecutor;
import kpn.lib.saver.SaveService;

public interface BaseDTOService<E, R, L, I, P> {
    // SaveService<E, R> saver();
    // LoadService<R, L, I> loader();
    // DeleteService<R, I> deleter();
    // PredicateExecutor<R, P> executor();
}
